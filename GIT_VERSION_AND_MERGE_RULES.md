# Git 提交、版本与合并规范

本规范参考 TokenSpider 项目的提交约定，适用于 BehindWatch 的提交、推送、合并和版本发布。

## 1. 提交前检查

1. 检查实际 diff 和工作区状态，明确本次修改的目的与范围。
2. 只暂存本次任务相关文件；不得混入无关修改、临时文件、日志、缓存、密钥或本地环境文件。
3. 根据改动风险执行相关测试或最小验证。用户可见行为变化应验证主要使用路径。
4. 无法运行验证时，说明原因和已完成的静态检查，不得声称测试通过。
5. 一个提交围绕一个明确目的组织；相关实现、测试和必要文档可以一并提交。

## 2. Commit 格式

统一使用：

```text
类型: 中文简要说明
```

标题应说明具体对象和行为变化。复杂修改可在正文补充原因、影响范围和验证结果。

| 类型 | 用途 |
| --- | --- |
| `feat` | 新增功能 |
| `fix` | 修复 Bug |
| `perf` | 性能优化 |
| `refactor` | 不改变功能的代码重构 |
| `ui` | 界面或交互调整 |
| `docs` | 文档更新 |
| `style` | 不影响行为的代码格式调整 |
| `build` | 构建、打包或依赖调整 |
| `chore` | 其他维护性修改 |

示例：

```text
feat: 新增监控窗口选择功能
fix: 修复停止监控后仍触发提醒的问题
perf: 减少摄像头画面处理中的重复计算
ui: 调整监控设置区域布局
docs: 添加 Git 提交与发布规范
```

禁止使用“修改一下”“更新代码”“修复问题”“提交”“优化”等无法说明实际变化的标题。

## 3. 分支与合并请求

新建分支默认使用 `codex/` 前缀，并描述用途，例如：

```text
codex/fix-monitor-stop
codex/feat-window-selection
codex/docs-git-rules
```

用户明确指定分支名时遵循用户要求。不因提交而擅自重命名现有分支。

合并请求标题采用与 Commit 相同的格式。正文应说明：

- 修改类型、解决的问题和修改后的行为。
- 主要修改内容与影响范围。
- 实际执行的验证及结果。
- 必要的兼容性说明、未解决风险和版本变化。

合并前确认源分支、目标分支和实际差异，不使用含义模糊的标题或说明。

## 4. 版本管理

版本号采用 `主版本号.次版本号.修订号`，用于标识公开发布，不用于标识每次提交。

- 普通开发提交不单独递增公开版本号。
- 一个发布批次可以包含多次提交；准备创建版本 Tag 或 GitHub Release 时，根据整个批次统一确定版本号。
- 不兼容变更或重大升级增加主版本号；兼容的新功能批次增加次版本号；Bug 修复和细节优化为主的发布增加修订号。
- 同一批次已经更新版本号但尚未发布时，后续修改合并到该批次，不重复递增。
- 版本段按整数递增，例如 `1.9.0` 的下一个次版本是 `1.10.0`，不因达到 9 而升级主版本。
- 优先复用项目已有版本来源；尚未建立版本机制时，不为普通提交额外引入版本文件或发布流程。

## 5. 发布说明

正式发布时，优先复用已有 Release 文案和模板；若尚无约定，使用 `release-notes/v<版本号>.md` 保存发布说明。

发布说明至少包含更新类型、主要变更和验证结果；必要时补充兼容要求、安装包和校验信息。不得填写未实际执行的验证结果。

README 保留项目介绍、使用说明和必要的当前版本信息，不新增版本更新流水账。

创建 GitHub Release 后，应实际核对正文、版本 Tag 和预期附件，不能只确认上传成功。

## 6. 提交、推送与发布的执行范围

- 用户只要求“提交”时，完成本地 Commit；不自动推送或发布。
- 用户要求“提交并推送”时，确认远程和目标分支，完成提交并推送。
- 参考 TokenSpider 的约定：只有仓库已配置版本 Tag 触发 Release、当前批次已准备好版本号与对应发布说明且适合发布时，“提交并推送”才默认同时创建并推送对应版本 Tag。用户明确要求不发版或不创建 Tag 时，以用户要求为准。
- 不覆盖已有发布 Tag，不擅自强制推送或改写已共享的提交历史。
- 推送或发布失败时，如实说明原因和未完成步骤，不得将本地成功描述为远程成功。

## 7. 完成后的汇报

根据实际执行范围，简要说明：

- 修改类型和主要修改内容。
- 实际验证结果及未解决事项。
- 提交分支、Commit 短哈希和标题。
- 若执行推送或合并，说明远程目标分支及实际结果。
- 若执行发布，说明版本号、Tag 和发布状态。

未执行的操作应标明“未执行”或省略，不得只用“已完成”“已推送”代替具体结果。
